[project]
name = "ledger-lens-rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Vishal Soni", email = "iitr.aiops.group.12@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
ledger-lens-rag = "ledger_lens_rag:main"

[build-system]
requires = ["uv_build>=0.11.19,<0.12.0"]
build-backend = "uv_build"
