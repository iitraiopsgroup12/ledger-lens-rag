apiVersion: v1
kind: Service
metadata:
  name: {{ include "ledger-lens-rag-chart.fullname" . }}
  labels:
    {{- include "ledger-lens-rag-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ledger-lens-rag-chart.selectorLabels" . | nindent 4 }}
